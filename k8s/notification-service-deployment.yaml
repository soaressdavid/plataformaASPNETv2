apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: aspnet-learning-platform
  labels:
    app: notification-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
      - name: notification-service
        image: aspnet-learning-platform/notification-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: ASPNETCORE_ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: platform-config
              key: ASPNETCORE_ENVIRONMENT
        - name: ASPNETCORE_URLS
          valueFrom:
            configMapKeyRef:
              name: platform-config
              key: ASPNETCORE_URLS
        - name: REDIS_HOST
          valueFrom:
            configMapKeyRef:
              name: platform-config
              key: REDIS_HOST
        - name: REDIS_PORT
          valueFrom:
            configMapKeyRef:
              name: platform-config
              key: REDIS_PORT
        - name: ConnectionStrings__DefaultConnection
          valueFrom:
            secretKeyRef:
              name: platform-secrets
              key: SQL_CONNECTION_STRING
        - name: RABBITMQ_HOST
          valueFrom:
            configMapKeyRef:
              name: platform-config
              key: RABBITMQ_HOST
        - name: RABBITMQ_PORT
          valueFrom:
            configMapKeyRef:
              name: platform-config
              key: RABBITMQ_PORT
        - name: SENDGRID_API_KEY
          valueFrom:
            secretKeyRef:
              name: platform-secrets
              key: SENDGRID_API_KEY
        - name: SMTP_HOST
          valueFrom:
            configMapKeyRef:
              name: platform-config
              key: SMTP_HOST
              optional: true
        - name: SMTP_PORT
          valueFrom:
            configMapKeyRef:
              name: platform-config
              key: SMTP_PORT
              optional: true
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: aspnet-learning-platform
  labels:
    app: notification-service
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: notification-service
